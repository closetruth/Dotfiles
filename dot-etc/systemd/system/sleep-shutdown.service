[Unit]
Description=Sleep ShutDown
# 确保基础服务已启动（可选）
After=network.target  

[Service]
Type=oneshot

# 脚本
ExecStart=/usr/local/bin/sleep-shutdown.bash  

# 避免超时等待
TimeoutStartSec=0      

# 服务执行后不保留状态
RemainAfterExit=no     

[Install]
# 在系统启动到多用户模式时触发
WantedBy=multi-user.target  
